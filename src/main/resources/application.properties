# Server port is arbitrary
server.port=8082

# TODO - Provide the appropriate connection url and credentials for Spring to configure your external db as the primary data source.
# Data Source Setup
# Note: Users must create a mysql db that they will reference here, create a user that has access to it on localhost
#       and provide the credentials below to access it

spring.datasource.url=jdbc:mysql://localhost:3307/critterdb
spring.datasource.username=critter-user
spring.datasource.password=critterpsw
spring.datasource.initialization-mode=always
spring.jpa.hibernate.ddl-auto=create-drop

# uncomment the below lines to view sql output in console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true